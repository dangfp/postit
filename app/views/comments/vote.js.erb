<% if @vote.valid? %>
  $("#comment_<%= @comment.id %>_votes").html("<%= @comment.total_votes %> votes");
<% else %>
  $("#comment_<%= @comment.id %>_votes_error").show().html("You can only vote on a comment once.");
<% end %>