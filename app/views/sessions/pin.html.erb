<%= render 'shared/content', title: "Enter your pin to log in" %>
<p>Your account is locked. You will receive a text message with a pin number.  Enter the pin number to unlock your account and proceed with login.</p>

<div class="row">
  <div class="span8 well">
    <%= form_for(:session, url: pin_path) do |f| %>
      <div class="control-group">
        <%= f.label :pin, 'Pin Number' %>
        <%= f.text_field :pin %>
      </div>
      <%= f.submit 'Unclock account', class: 'btn btn-success' %>
    <% end %>
  </div>
</div>