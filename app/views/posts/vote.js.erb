<% if @vote.valid? %>
  $("#post_<%= @post.slug %>_votes").html("<%= @post.total_votes %> votes");
<% else %>
  $("#post_<%= @post.slug %>_votes_error").show().html("You can only vote on a post once.");
<% end %>